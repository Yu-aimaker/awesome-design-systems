# AwesomeDS

Generic design system canon for AI coding agents. White ground, dark ink, blue primary.

- `DESIGN.md` — contract
- `tokens.json` / `tokens.css` — values
- `apps/web` — docs + gallery that use this canon

No product-specific branding. Future OSS ready.
